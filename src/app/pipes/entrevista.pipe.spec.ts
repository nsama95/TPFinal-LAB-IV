import { EntrevistaPipe } from './entrevista.pipe';

describe('EntrevistaPipe', () => {
  it('create an instance', () => {
    const pipe = new EntrevistaPipe();
    expect(pipe).toBeTruthy();
  });
});
